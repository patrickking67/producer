---
description: Release prep — master targets, distribution metadata, artwork, ISRC, schedule
argument-hint: "<track title, or 'releasing X on Y label'>"
---

Invoke the `release-prep` skill. Walk through master target (streaming vs. club), metadata (title, artist, ISRC, BPM, key, genre tags), artwork dimensions, label deadlines. Optionally schedule release-day reminders in Google Calendar and draft promo emails via Gmail.
