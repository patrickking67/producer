---
description: Browse the Ableton Library — find presets, samples, packs, drum kits, racks across your User Library and installed Packs
argument-hint: "<sound, preset, drum kit, or pack name>"
---

Invoke the `library-browser` skill. Walk the user's local Ableton User Library and installed Packs the way the Live browser does — by category, by name, by tag. If nothing matches locally, offer a Splice fallback.
