---
description: Compose a MIDI part — chords, bassline, drum pattern, melody. Saves a .mid file.
argument-hint: "<key, tempo, vibe, length>"
---

Invoke the `midi-lab` skill. Always output a human-readable recipe table *and* save an actual `.mid` file to the user's Music folder. If the Ableton MCP is connected, offer to drop the clip straight into the highlighted clip slot.
