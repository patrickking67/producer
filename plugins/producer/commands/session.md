---
description: Drive Ableton Live directly — create tracks, drop devices, set tempo/key, fire clips, capture session state
argument-hint: "<what you want to do in Live>"
---

Invoke the `session-bridge` skill. Use the Ableton MCP tools to actually act on the running Live set — never narrate what you would do, do it. Confirm destructive actions (delete track, clear clip) before executing.
