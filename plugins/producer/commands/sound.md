---
description: Patch recipe or sound-design walkthrough in Ableton stock devices (or via Splice samples)
argument-hint: "<the sound you want to design>"
---

Invoke the `sound-design` skill. Lead with quick read + build steps + FX chain + variants. Reach for Splice when audio is faster than synthesis. If the user's Ableton Library has a matching preset, surface it via the `library-browser` skill first.
